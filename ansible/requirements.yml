# Collections the playbooks use.
#
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: amazon.aws
    version: ">=10.0.0"
  - name: community.aws
    version: ">=10.0.0"
  - name: kubernetes.core
    version: ">=6.0.0"
  - name: community.mysql
    version: ">=4.0.0"
